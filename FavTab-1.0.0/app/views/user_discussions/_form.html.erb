<%= form_for(@user_discussion) do |f| %>
  <% if @user_discussion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_discussion.errors.count, "error") %> prohibited this user_discussion from being saved:</h2>

      <ul>
      <% @user_discussion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.text_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :discussion_id %><br />
    <%= f.text_field :discussion_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
